运行
```bash
uvicorn TimeServerFastAPI:app --host 127.0.0.1 --port 8001 --reload
```
调试
```bash
npx @modelcontextprotocol/inspector uvx mcp-serverDemo-fetch
```
CurrentTimeServerSSE3.py
```bash
uv run CurrentTimeServerSSE3.py --reload
```
client
```bash
uv run sse_client.py http://127.0.0.1:8000/sse
```
